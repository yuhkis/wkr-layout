# 公開ベータの検証記録

## 2026-09-21 — 公開準備

配列 **2.0.0-beta.1**、練習 **0.1.0**。配列の正本JSONは233規則です。WKR macOS Publicはこのリポジトリの固定commitを取り込み、配列・アプリ・練習の版を別々に表示します。

| 検証 | 結果 | 範囲 |
| --- | --- | --- |
| `python3 scripts/generate.py --check` | 成功 | 正本、配列表、v2 IMEテーブル、教材が一致 |
| `python3 -m unittest discover -s tests` | 7件成功 | 全233規則、接頭辞、全127問の綴り、テーブル入力の一意性 |
| `node --test practice/core.test.cjs` | 3件成功 | 保存許可項目、未知項目・異版・破損値、上限、課題集計 |
| Chromium、file URL、ネットワークオフライン | 成功 | 全12課題・127問を完走。各課題で合成した誤キーも使用。HTTP(S)リクエスト0件 |
| 保存・再読込・削除 | 成功 | opt-in、課題別完了回数・最高正答率だけの保存、復元、削除後に保存キーが残らないこと |
| IMEイベント境界 | 成功 | 合成したcomposition中のEnterを採点せず、確定後の別Enterで採点。誤入力欄をクリア |
| 幅1150 / 390の画面 | 確認 | 課題・入力欄・保存説明が読める。狭い画面の横幅超過を修正 |
| macOS同梱WKWebView | 確認 | 同じファイルを読み、12課題・短文・版表示・保存オフ・保存/削除を画面で確認 |

「キー位置」は英字キーイベントの練習です。合成compositionイベントの検査は実IMEの互換性確認を置き換えません。

未確認: Google日本語入力・azooKeyでのインポートと実入力、実機のApple日本語入力の変換候補・確定・削除、JIS以外の記号位置、Safari / Firefoxでの全課題完走、支援技術による読み上げ操作。日常用のアプリや入力設定はこのPublic作業では切り替えていません。

仕様zipとオフライン練習zipは `scripts/package.py` でcleanなcommitから生成し、元commitをmanifestへ、各配布物のSHA256を `SHA256SUMS` へ記録します。公開前の履歴監査は [publication-audit.md](publication-audit.md) を参照してください。

## 2026-09-21 — 独立履歴での公開準備

公開用ファイルだけから始まる独立履歴を公開候補とします。正本JSON、v1 / v2のIMEテーブル、教材、練習アプリとテストの内容は、上記検証時のファイルとハッシュで一致を確認しました。v1資料のリンクを同梱テーブルへ修正し、仕様zipにもそのテーブルを含めます。新しい作業場所で生成一致、Python 7件、Node 3件の成功を再確認しました。実IMEの未確認範囲は上記のままです。

## 公開操作の回帰検査

2026-09-21、`python3 scripts/test_publication_guard.py` の15件が成功しました。合成の一時Gitで、削除済み連絡先の過去履歴、他のroot、未許可パス・identity、監査後のcommit/tag/配布物変更、別URL・repository ID、mainへの付け替え、非fast-forwardを拒否します。実際のpre-push hookが未承認のpushを拒否し、試験用の宛先へrefを送らないことも確認しました。検査は内容を読む公開前監査と併用します。

## 2026-09-21 — macOS対応版と実機確認範囲の補足

対応アプリは[WKR macOS v2 Public Beta 0.8.0-public.beta.5](https://github.com/yuhkis/wkr-macos/releases/tag/v0.8.0-public.beta.5)です。配列2.0.0-beta.1と練習帳0.1.0を使います。

Public beta.1で権限付与と短い日本語の実変換、beta.2で同梱練習の課題と配列図表示を利用者が確認しています。macOSでの確認実績がすべて未了という意味ではありません。beta.5は同じ変換処理・教材を引き継いでいますが、この配布版そのものの実機入力・停止復帰は再確認待ちです。[アプリ側の検証記録](https://github.com/yuhkis/wkr-macos/blob/main/docs/verification.md)に版ごとの範囲を記載しています。

Google日本語入力とazooKeyのテーブルは別の入力経路です。Apple日本語入力とWKRの実変換を、これらのテーブルの実IME確認として扱いません。


## 2026-09-22 — 対応アプリの表記

対応アプリは「WKR macOS v2 Public Beta」と表記します。Public Betaは一般公開の試験版を表し、正規タグ `v0.8.0-public.beta.5` と配布済みファイルは維持します。別名タグ `v0.8.0-beta.5` も同じ公開コミットを指し、以前の検証範囲は保持します。


## 2026-09-22 — 別名タグの案内を撤回

前記「対応アプリの表記」にある別名タグの案内を訂正します。`beta.5` と `public.beta.5` は公開段階の意味が異なるため、追加した `v0.8.0-beta.5` を撤回し、正規タグ `v0.8.0-public.beta.5` に統一します。

文書とReleaseの案内を修正する変更です。アプリ・配列・教材の実装や配布済みファイルは変更せず、従来の動作検証範囲と未確認事項を引き継ぎます。


## 2026-09-22 — Web練習帳0.2.0のデモ公開候補

対応配列は2.0.0-beta.1のままです。新しいWeb版ではQWERTYからページ内でかなを入力でき、WKR・IMEから確定したひらがなを受け取るモードも選べます。公開済みmacOSアプリと既存Releaseの練習帳0.1.0は変更しません。

- 生成一致検査、Python 7件、Node 9件、公開ガードの合成データによる22件が成功しました。全233規則と全127問の綴りを検査しています。
- Chromiumで母音9問を完了し、合成した誤答後の再入力、自由入力の「あ」「き」「わから」、Backspace、消去を確認しました。
- 成績保存の既定オフ、有効化後の課題別完了回数・最高正答率の復元、削除して再読込した後の成績消去・保存オフを確認しました。入力本文は保存検査用のデータに使いません。
- WKR・IMEモードへの確定済みひらがなの入力と採点、モード切り替え時の両入力欄の消去を確認しました。これは実IMEの変換候補・composition境界を検証したものではありません。
- 公開するサイトは固定9ファイルです。mainから手動実行するPages workflowは事前監査したZIPのSHA256一致を必須にし、commitやPRだけでは配信しません。

未確認: このWeb版と実機WKR・Apple日本語入力を組み合わせたcomposition操作、Google日本語入力・azooKeyでの実入力、US配列の記号、タッチキーボード、Safari / Firefoxでの全課題完走、支援技術での操作。自動検査・ブラウザ操作と実IMEの確認を区別します。


## 2026-09-26 — 配列2.0.0-beta.2（ん＝N、っ＝M に戻す）と練習帳0.2.1

2.0.0-beta.1で入れ替えていた ん・っ をv1と同じ位置へ戻す変更です。ほかの規則は変えていません。Release・タグ・ブラウザデモは更新していません。

- 正本JSONの差分は規則2件（`n-n`：N→ん、`m-small-tsu`：M→っ）と配列版だけです。規則数は233件、記号55件のままです。
- `python3 scripts/generate.py --check` で配列表・v2のIMEテーブル・練習の `data.js` の生成一致を確認しました。生成物の差分は、配列表の2行と配列版、Google日本語入力・azooKeyの各2行、練習データの配列版・練習版・キーボード図のN/M・打鍵列だけです。
- Python 8件（N/Mの規則IDと、Pの後の `N`＝′・`M`＝″ が変わらないことの検査を追加）、Node 9件が成功しました。全教材の綴りは同じ233規則で再変換して対象かなと一致します。
- 教材の打鍵が変わった課題は、ん・っ を含む5課題です（濁音、ん・っ・長音、身近な言葉、短い文、v1からの移行）。「v1 からの移行」からは ん・っ の問題を外し、説明文の ん・っ の位置を直しました。ページ下部の版の説明は「0.2.0からQWERTY体験を追加」に直しました。
- ローカルに配信したページをChromium系のブラウザで開き、版表示「練習 0.2.1 / 配列 2.0.0-beta.2」、キーボード図の N＝ん・M＝っ、QWERTY体験の体験欄で `E` `K` `N` が「きん」、`E` `K` `M` が「きっ」になることを確認しました。

未確認: 実機WKR・Apple日本語入力との組み合わせ、Google日本語入力・azooKeyでの実入力、Safari / Firefox。WKR macOS側の同期（別リポジトリ）は未実施です。


## 2026-09-26 — 配列2.0.0-beta.2・練習帳0.2.1のRelease・デモ公開候補

配列2.0.0-beta.2と練習帳0.2.1を、Release（仕様ZIP・練習帳ZIP・`manifest.json`・`SHA256SUMS`）とブラウザデモで公開する準備です。配列・教材の内容は前項から変えていません。

- `scripts/package.py` は、Release済みの配列版（2.0.0-beta.1）の仕様ZIPを作らず、まだReleaseしていない配列版だけ仕様ZIPを作ります。収録パスは2.0.0-beta.1の仕様ZIPと同じ固定リストで、その版の `docs/release-<配列版>.md` を加えます。ZIP内のREADMEは配布用の短い説明です。
- 文書は公開後の状態に合わせました（README、対応表、練習帳の説明、Release文案）。公開済みのmacOSアプリは2.0.0-beta.1のままであることを明記しています。
- 配布物とサイトは統合後のmainのcommitから作り、公開ガードの監査と、公開物のSHA256の照合を行います。


## 2026-09-27 — 配列2.0.0-beta.3（ん＝N・U、や＝P、特殊列の廃止、記号・矢印＝Q Q）と練習帳0.3.0

- 正本JSONは220規則（かな161、記号55、矢印4）。2.0.0-beta.2から変わらない規則は142件です。記号55件・矢印4件は、先頭の `P` を `Q` `Q` に替えたこと以外（後ろのキー・出力・送り方・種別）が2.0.0-beta.2と同じであることを生成時に照合しました。
- `python3 scripts/generate.py --check` で配列表・キー配置の図（`docs/images/keymap-v2.svg`）・v2のIMEテーブル・練習の `data.js` の生成一致を確認しました。配列表の冒頭に、キー配置の表と五十音順の組み合わせ表を生成しています。
- Google日本語入力のv2テーブルでは、出力を持たない2打鍵の接頭辞 `Q Q` を、「ぁ」の保留から記号の保留（■）へ移る行で表しました（それまでの生成処理は1打鍵の接頭辞だけを想定していました）。
- Python 10件、Node 9件が成功しました。全教材の綴りは同じ220規則で再変換して対象かなと一致します。ん を含む問題の打鍵は `U` で示すことを検査しています。
- ローカルに配信したページをChromium系のブラウザで開き、版表示「練習 0.3.0 / 配列 2.0.0-beta.3」、キーボード図の `U`＝ん・`P`＝や・`Q`＝ぁ、課題が11件で特殊列の課題がないこと、課題5の ん が `U` で示されることを確認しました。
  QWERTY体験の体験欄で `E P` `E U` `E H P` `Q Q L` `N` `M` が「きゃかんかや→んっ」になりました。コンソールのエラーは0件です。
- 公開ガードの合成データによる検査と、公開前の全履歴・GitHub上の全表面の監査は、公開操作の記録（Git管理外）に残します。

未確認: 実機WKR・Apple日本語入力との組み合わせ、Google日本語入力・azooKeyでの実入力（特に `Q Q` の保留の扱い）、Safari / Firefox。WKR macOS側の同期（別リポジトリ）は未実施です。
